# PRD — 맛집 리뷰 서비스 (가칭)

> **작성일** 2026-08-14
> **문서 상태** Phase 0(랜딩페이지) 구현 착수용 확정본
> **오늘 만드는 것** 랜딩페이지 1장 + 체험 데모

---

## 0. 한눈에 보기

| 항목 | 내용 |
|---|---|
| 서비스 | 담아둔 맛집 중에서 오늘 갈 곳을 골라주는 서비스 |
| 타깃 | 20~30대 직장인 |
| 핵심 가치 | 검색이 아니라 **결정** |
| 오늘의 범위 | 랜딩페이지 1장 (섹션 7개) + 체험 데모 1개 |
| 오늘의 스택 | 단일 HTML + Vanilla JS + CSS, 데이터 하드코딩 |
| 오늘 안 하는 것 | 로그인, DB, AI, 실제 저장 |

---

## 1. 서비스 정의

### 한 문장 정의

> **20~30대 직장인이, 주말에 모아둔 맛집 목록에서 평일 점심에 오늘 갈 곳을 3초 안에 고를 수 있게 해주는 서비스**

| 항목 | 내용 |
|---|---|
| 누가 | 20~30대 직장인 |
| 언제 | 주말(맛집을 모아둘 때) + 평일 점심(회사 근처에서 빨리 골라야 할 때) |
| 왜 | 저장해둔 곳은 많은데, 그중 뭐가 내 취향에 맞는지 판단할 기준이 없어 결국 못 고르기 때문 |

### 문제 정의

기존 지도·리뷰 서비스는 **저장(모으기)까지는 잘 해주지만, 꺼내 쓰는 단계가 비어 있다.**
그 결과 저장 목록은 계속 길어지는데, 정작 배고픈 12시에 열면 여전히 고르지 못한다.

→ 이 서비스가 해결하는 지점은 **검색이 아니라 결정**이다.

### 서비스 구조 — 두 개의 층

| 층 | 역할 | 대상 데이터 | 관련 기능 |
|---|---|---|---|
| **탐색층** | 갈 만한 곳을 발견하고, 담을지 판단 | 전체 사용자 리뷰 | F5, F6 |
| **결정층** | 담아둔 곳 중 오늘 갈 곳을 고름 | 내 목록 (가볼 곳 / 가본 곳) | F3, F4 |

- 경쟁 서비스(네이버 지도·카카오맵·다이닝코드)는 대부분 **탐색층까지만** 제공한다.
- 이 서비스의 고유 가치는 **결정층**에 있다. → **랜딩의 헤드라인과 체험 데모는 결정층에 맞춘다.**
- AI 요약(F6)은 탐색층에서 광고성 리뷰의 노이즈를 걸러 판단을 돕는 역할이다.
- 리뷰의 목적은 "평가 점수"가 아니라 **"다음에 또 갈지 판단"**이다.

---

## 2. 사용자 시나리오

**① 주말 — 모으기**
인스타에서 본 파스타집을 담는다 → 상태 `가볼 곳`, 태그 `여럿` `분위기`

**② 평일 12시 — 꺼내기**
앱을 연다 → "오늘 뭐 먹지" → 조건 선택(`실패 없는 곳` `혼자` `가까움`) → 한 곳 추천 → 간다

**③ 점심 이후 — 기록**
"또 올 것 같다 / 한 번이면 충분" 선택 + 한 줄 → 상태가 `가본 곳`으로 전환 → 다음 추천에 반영

---

## 3. 핵심 기능 (전체 서비스)

| # | 기능 | 역할 | 구분 |
|---|---|---|---|
| F1 | 맛집 담기 | 목록의 입구. `가볼 곳` 상태로 등록 | 전제 |
| F2 | 상태 전환 + 방문 기록 | `가볼 곳` → `가본 곳`. 재방문 의사 중심의 짧은 리뷰 | 필수 |
| F3 | 오늘 뭐 먹지 | 조건을 넣으면 내 목록에서 한 곳을 골라줌 | **핵심 가치** |
| F4 | 태그 분류 | 골라주기의 조건이 되는 기준 (혼밥·회식·가성비 등) | 보조 |
| F5 | 리뷰 리스트 + 검색·필터 | **전체 사용자 리뷰** 탐색. 초기 데이터는 JSON 시드 | 확장 |
| F6 | AI 리뷰 요약 · 감성 분석 | 리뷰 텍스트에서 요약·긍부정 추출 (Gemini API) | 확장 |
| F7 | 로그인 · 데이터 저장 | 계정 기반 영속 저장 (Supabase) | 인프라 |
| F8 | 대시보드 | 도넛 차트(취향 분포) + 워드클라우드(자주 쓴 표현) | 확장 |

### 의존 관계

```
F7 로그인·저장 ──> F1 담기 ──> F2 기록 ──> F5 리뷰 리스트
                                  │
                                  ├──> F3 골라주기
                                  ├──> F6 AI 요약·감성분석 ──> F8 대시보드
                                  └──> F4 태그 ───────────────┘
```

- **F7이 사실상 모든 기능의 전제다.** 저장이 없으면 F5·F6·F8은 보여줄 데이터가 없다.
- **F6·F8은 리뷰가 쌓인 뒤에야 의미가 생긴다.** 초기 사용자는 빈 대시보드를 보게 되므로, 시드 데이터 또는 "리뷰 3개부터 열립니다" 같은 진입 조건이 필요하다.

### 데이터 모델 개념

맛집 하나 = 카드 하나. 카드는 상태를 가진다.

```
[가볼 곳] ──(방문 기록 작성)──> [가본 곳]
```

- `가볼 곳` → "새로운 곳" 추천 대상
- `가본 곳` → 재방문 의사 데이터 보유 → "실패 없는 곳" 추천 대상

### 공개 리뷰 도입에 따른 신규 과제 *(Phase 3 이전 결론 필요)*

| 과제 | 내용 |
|---|---|
| 콜드 스타트 | 초기 리뷰 0개. 시드 데이터의 규모·기준 결정 필요 |
| 리뷰 신뢰성 | 광고 리뷰를 걸러내겠다고 약속한 이상, 공개 리뷰에도 같은 기준 필요 |
| 신고 · 어뷰징 | 공개 게시물이 되는 순간 신고·삭제·비속어 정책 필요 |
| 작성자 노출 범위 | 닉네임만인지, 타인의 목록까지 공개인지 |

---

## 4. 오늘의 범위 (Phase 0)

### 만드는 것

- 랜딩페이지 1장 (섹션 7개 — 5장 참고)
- 체험 데모 1개 (취향 질문 3개 → 샘플 맛집 1곳 추천 — 6장 참고)

### 만들지 않는 것

- 회원가입 / 로그인 (F7)
- 실제 맛집 담기 · 저장 (데이터 영속성 없음)
- 방문 기록 작성 (F2)
- 리뷰 리스트 · 검색 · 대시보드 (F5, F8)
- Gemini API 호출 (F6)
- Supabase 연동 (F7)
- 지도 API · 위치 기반 거리 계산
- 사전 신청 이메일 수집

> ⚠️ **체험 데모의 추천은 규칙 기반 필터링이며 AI가 아니다.** 랜딩에 "AI가 추천해드려요"라고 쓰지 않는다. 지금 못 지키는 약속을 카피에 넣으면 나중에 어긋난다.

---

## 5. 랜딩페이지 화면 구성

| 순서 | 섹션 | 목적 |
|---|---|---|
| 1 | Hero | 3초 안에 이해시키기 |
| 2 | 문제 공감 | "내 얘기다" 만들기 |
| 3 | 해결 방식 | 구조 이해 |
| 4 | **체험 데모** | 가치를 직접 느끼게 하기 |
| 5 | 기능 미리보기 | 앞으로의 그림 |
| 6 | 마무리 CTA | 다음 행동 유도 |
| 7 | 푸터 | 마감 |

> **섹션 4가 이 랜딩의 주인공이다.** 1~3은 4로 데려가기 위한 도입, 5~6은 마무리다.

### 섹션별 상세 (카피는 초안 — 수정 가능)

**① Hero**
- 헤드라인: `저장은 했는데, 오늘 어디 가지?`
- 서브카피: `담아둔 곳이 있으면 골라드리고, 없으면 담는 것부터 도와드려요.`
- CTA 버튼: `오늘 갈 곳 골라보기` → 클릭 시 섹션 4로 스크롤 + 데모 시작
- 보조 텍스트 링크: `먼저 맛집부터 담을래요` → 담기 페이지(F1)
- 스크롤 유도 표시(아래 화살표 등)

> **v2 개정.** 초판 서브카피는 `담아둔 맛집 중에서 오늘 갈 한 곳을 골라드립니다.`였다.
> 담아둔 곳이 하나도 없는 첫 방문자에게는 지킬 수 없는 약속이라, 두 상태를 모두 말하도록 고쳤다.
> 보조 링크도 같은 이유로 새로 생긴 자리다 — 위계는 **버튼이 아니라 텍스트 링크**로 지킨다
> (DESIGN 8장 「랜딩에서 담기 페이지로 가는 입구」).

**② 문제 공감**
- 소제목: `저장한 맛집은 늘어나는데, 점심은 늘 가던 곳`
- 공감 포인트 3개 (카드 또는 리스트)
  - 저장 목록이 길어질수록 열어보기 싫어진다
  - 뭐가 내 취향이었는지 기억이 안 난다
  - 결국 어제 갔던 데를 또 간다

**③ 해결 방식 — 3단계 카드**

| 단계 | 제목 | 설명 |
|---|---|---|
| 1 | 담는다 | 가보고 싶은 곳을 모아둔다 |
| 2 | 가보고 남긴다 | "또 올까?" 한 줄만 기록한다 |
| 3 | 골라준다 | 오늘 조건에 맞는 한 곳을 꺼내준다 |

**④ 체험 데모** → 6장 상세 스펙

**⑤ 기능 미리보기** — 각 항목에 `준비 중` 배지

| 표시할 기능 | 한 줄 설명 |
|---|---|
| 맛집 담기 (F1) | 링크 하나로 목록에 추가 |
| 방문 기록 (F2) | 또 갈지 안 갈지만 남기면 끝 |
| 리뷰 탐색 (F5) | 다른 사람들의 기록에서 찾아보기 |
| 취향 대시보드 (F8) | 내가 어떤 걸 좋아하는지 한눈에 |

**⑥ 마무리 CTA**
- 문구: `오늘 점심, 더 고민하지 마세요`
- 버튼: `다시 골라보기` → 섹션 4로 스크롤 + 데모 초기화
- (사전 신청 자리는 비워두되, 추후 삽입 가능하도록 마크업만 준비)

**⑦ 푸터**
- 서비스명 · 제작 정보 · 연도

---

## 6. 체험 데모 상세 스펙

### 흐름

```
[시작] → Q1 → Q2 → Q3 → 결과 카드 1곳 → [다시 하기]
```

- 한 화면에 질문 하나씩 노출한다.
- 선택하면 **자동으로 다음 질문으로 전환**된다. (다음 버튼 없음)
- 진행 표시 `1 / 3` 형태로 노출한다.
- 뒤로 가기 1단계는 허용한다.

### 질문 구성

| # | 질문 | 선택지 | 매칭 기준 |
|---|---|---|---|
| Q1 | 오늘은 어떤 기분인가요? | 새로운 곳 / 실패 없는 곳 | `status`: `wish` / `visited` |
| Q2 | 누구랑 가세요? | 혼자 / 동료랑 | `tags`에 `혼밥` / `여럿` 포함 |
| Q3 | 뭐가 제일 중요해요? | 가까움 / 가성비 / 분위기 | `tags`에 해당 값 포함 |

### 매칭 로직

```
1. Q1 조건으로 status 필터
2. Q2 조건으로 태그 필터
3. Q3 조건으로 태그 필터
4. 결과가 0곳이면 → Q3 조건 해제 후 재시도 (안내 문구 표시)
5. 그래도 0곳이면 → Q2 조건 해제 후 재시도 (안내 문구 표시)
6. 결과가 여러 곳이면 → 랜덤 1곳 (직전에 보여준 곳은 제외)
```

- 조건 완화가 일어나면 결과 카드에 밝힌다: `조건을 조금 넓혀서 골랐어요`
- 빈 결과 화면은 만들지 않는다. 체험형 랜딩에서 가장 치명적인 순간이다.

### 결과 카드 구성

- 맛집 이름 + 카테고리
- 태그 3개
- 한 줄 평
- **상태별 하단 표시**
  - `visited` → `또 갈 것 같아요 82%`
  - `wish` → `아직 안 가본 곳` 배지
- 추천 이유 한 줄 — 선택한 조건을 조합해 생성
  예: `혼자 가기 좋고, 가까워서 골랐어요`
- 버튼: `다시 하기`

### 샘플 데이터 (10곳 — 하드코딩)

> 실존 매장에 대해 사실이 아닌 평가를 만들어내지 않기 위해 **전부 가상의 매장명**을 사용한다.

```js
const PLACES = [
  // 가본 곳
  { name: "담백한식탁",   category: "한식",   status: "visited", tags: ["혼밥","가까움","가성비"], oneLiner: "혼자 가서 20분 만에 먹고 나오기 좋아요", revisitRate: 88 },
  { name: "라멘 코지",     category: "일식",   status: "visited", tags: ["혼밥","가까움","분위기"], oneLiner: "카운터석이 조용해서 혼자 앉기 편해요",   revisitRate: 82 },
  { name: "화로상회",       category: "고기",   status: "visited", tags: ["여럿","분위기"],          oneLiner: "회식하기 딱 좋은 자리 간격",             revisitRate: 79 },
  { name: "골목분식",       category: "분식",   status: "visited", tags: ["혼밥","여럿","가성비"],   oneLiner: "가격 생각하면 다시 갈 수밖에 없어요",   revisitRate: 85 },
  { name: "테라스 델피",   category: "양식",   status: "visited", tags: ["여럿","분위기","가까움"], oneLiner: "창가 자리가 좋아서 손님 데려가기 좋아요", revisitRate: 76 },

  // 가볼 곳
  { name: "미도리 스시",   category: "일식",   status: "wish",    tags: ["혼밥","분위기"],          oneLiner: "조용한 오마카세라고 들었어요",           revisitRate: null },
  { name: "마라공방",       category: "중식",   status: "wish",    tags: ["여럿","가성비"],          oneLiner: "양 많고 저렴하다는 얘기가 많아요",       revisitRate: null },
  { name: "브레드 앤 스푼", category: "브런치", status: "wish",    tags: ["혼밥","가까움","가성비"], oneLiner: "회사에서 걸어서 5분 거리",               revisitRate: null },
  { name: "루프탑 하루",   category: "이자카야", status: "wish",  tags: ["여럿","분위기","가까움"], oneLiner: "퇴근하고 한잔하기 좋아 보여요",         revisitRate: null },
  { name: "삼거리 곱창",   category: "곱창",   status: "wish",    tags: ["여럿","가성비","가까움"], oneLiner: "줄 선다는 얘기를 계속 듣고 있어요",     revisitRate: null }
];
```

> 이 구성은 Q1×Q2×Q3의 **12개 조합 전부에 최소 1곳이 매칭**되도록 설계했다. 데이터를 수정할 때 이 조건이 깨지지 않는지 확인할 것.

---

## 7. 로드맵

| 단계 | 내용 | 관련 기능 |
|---|---|---|
| **Phase 0 (오늘)** | 랜딩페이지 + 체험 데모 | F3 축소판 |
| Phase 1 | 맛집 담기 · 방문 기록 · 태그 (localStorage) | F1, F2, F4 |
| Phase 2 | 내 목록 기반 골라주기 — 데모를 실기능으로 전환 | F3 |
| Phase 3 | 리뷰 리스트 + 검색·필터 (JSON → DB) | F5 |
| Phase 4 | 로그인 · Supabase 이관 | F7 |
| Phase 5 | AI 리뷰 요약 · 감성 분석 | F6 |
| Phase 6 | 대시보드 (도넛 차트 · 워드클라우드) | F8 |
| Phase 7 | 지도 API 연동, 위치 기반 거리 계산 | — |

### 배치 근거

- **localStorage를 먼저 쓰는 이유**: 데이터 구조를 확정한 뒤 DB로 옮기는 편이, 스키마가 흔들리는 상태에서 DB부터 잡는 것보다 안전하다. 로그인이 급하면 Phase 3과 4를 맞바꿔도 된다.
- **F6 → F8 순서**: 워드클라우드와 감성 도넛 차트는 F6의 분석 결과를 재료로 쓴다. F6 없이 F8을 만들면 단순 태그 집계에 그친다.

---

## 8. 기술 스택

### Phase 0 (오늘 — 랜딩)

| 항목 | 내용 |
|---|---|
| 구조 | 단일 HTML 파일 |
| 스크립트 | Vanilla JS (프레임워크 없음) |
| 스타일 | CSS (프레임워크 없음) |
| 데이터 | JS 배열 하드코딩, 저장 없음 |
| 반응형 | 모바일 우선. 데스크톱은 중앙 정렬 컨테이너 |
| 배포 | 미정 |

### Phase 1 이후

| 항목 | 내용 |
|---|---|
| 인증 · DB | Supabase (Auth + Postgres) |
| AI | Gemini API — 리뷰 요약, 감성 분석 |
| 차트 | 도넛 차트 · 워드클라우드 라이브러리 (선정 필요) |
| 데이터 | localStorage → JSON 시드 → Sㅊupabase 테이블 |

### ⚠️ 반드시 지킬 기술 제약

**Gemini API 키를 클라이언트 코드에 넣지 않는다.**
단일 HTML에서 `fetch`로 직접 호출하면 키가 그대로 노출되어 제3자가 소진시킬 수 있다.
→ Supabase Edge Function을 경유한다. 즉 **F6은 F7 없이 안전하게 구현할 수 없다.**

**AI 호출 비용·지연 관리**
리뷰마다 실시간 분석하면 비용과 대기 시간이 커진다. **저장 시점에 1회 분석하고 결과를 DB에 함께 저장**하는 방식을 기본으로 한다.

---

## 9. 디자인 가이드 (제안 — 확정 전)

| 항목 | 제안 |
|---|---|
| 무드 | 점심시간에 빠르게 쓰는 도구. 화려함보다 **가독성과 속도감** |
| 메인 컬러 | 따뜻한 오렌지 계열 1개 (CTA·강조 전용) |
| 배경 | 밝은 중성색. 카드는 흰색 + 은은한 그림자 |
| 폰트 | Pretendard 또는 시스템 산세리프 |
| 모서리 | 12~16px 라운드 |
| 애니메이션 | 질문 전환 시 페이드 + 살짝 슬라이드. 300ms 이내 |

---

## 10. 완료 기준 (Phase 0)

- [ ] 섹션 7개가 순서대로 렌더링된다
- [ ] Hero의 CTA를 누르면 데모 섹션으로 이동하고 첫 질문이 뜬다
- [ ] 질문 3개가 선택 즉시 다음으로 넘어간다
- [ ] 12개 조합 모두에서 결과 카드가 나온다 (빈 화면 없음)
- [ ] 조건 완화가 일어나면 그 사실이 카드에 표시된다
- [ ] "다시 하기"를 누르면 직전과 다른 결과가 나온다
- [ ] `wish` / `visited`에 따라 카드 하단 표시가 달라진다
- [ ] 모바일(375px)에서 가로 스크롤이 생기지 않는다
- [ ] 미구현 기능에 `준비 중` 배지가 붙어 있다
- [ ] "AI 추천" 등 지키지 못할 표현이 카피에 없다

---

## 11. 미확정 사항

**Phase 0 시작 전에 정하면 좋은 것**
- [ ] 서비스명
- [ ] 디자인 톤 최종 확정 (9장은 제안 상태)

**Phase 0 이후로 미뤄도 되는 것**
- [ ] 사전 신청 기능을 어느 단계에 넣을지
- [ ] F8 대시보드의 분석 대상 — 내 취향인가, 특정 맛집의 리뷰인가
- [ ] F6 감성 분석 결과의 노출 방식 (점수 / 라벨 / 요약문)
- [ ] 리뷰 신뢰성 확보 방안 (3장 신규 과제 참고)
- [ ] JSON 시드 데이터의 규모와 출처
- [ ] 차트 라이브러리 선정

**검증 필요**
- [ ] 타깃·문제 정의는 현재 **가설 상태**다. 실제 20~30대 직장인 인터뷰로 검증해야 한다.

**확정 완료**
- [x] F5 리뷰 리스트의 범위 → **전체 사용자 리뷰** (2026-08-14)
- [x] 골라주기의 대상 목록 → **가볼 곳 / 가본 곳 상태로 구분** (2026-08-14)
- [x] 랜딩의 목표 행동 → **체험 해보기** (2026-08-14)
- [x] 체험 방식 → **취향 질문 3개 → 샘플 맛집 추천** (2026-08-14)
