# DESIGN.md — 맛집 리뷰 서비스 (가칭)

> **문서 버전** v2 (2026-08-20) · 초판 2026-08-14
> **적용 범위** Phase 0 (랜딩페이지 + 체험 데모) · Phase 1 (맛집 담기 페이지)
> **상위 문서** `PRD.md` — 기능·범위 결정은 PRD가 우선하고, 시각 결정은 이 문서가 우선한다.
> **클래스 이름** `UI-CONTRACT.md` — 담기 페이지의 DOM 구조와 클래스 이름은 그쪽이 정본이다.

---

## 개정 이력

### v2 — 2026-08-20 · 맛집 담기 페이지(Phase 1) 추가

초판을 갈아엎지 않고 장을 덧붙였다. Phase 0 관련 서술은 한 줄도 바뀌지 않았다.

| 장 | 추가된 것 |
|---|---|
| 1 | `1.5 원칙의 적용 범위 — 결정층과 탐색층`. 「한 화면에 하나씩」이 담기 페이지에는 왜 적용되지 않는지 |
| 2 | `2.4 실제 배경 기준 대비표`. 토큰 표 값은 흰 배경 기준이라 그대로 믿으면 안 된다 |
| 4 | `4.5 담기 페이지 컨테이너` |
| 5 | `5.2 담기 페이지 컴포넌트` — 검색 폼 · 칩 · 상태 표시 · 검색 결과 카드 · 담기 버튼 · 담아둔 목록 · 토스트 |
| 7 | `7.2 담기 페이지 문구` |
| 8 | `8-B 담기 페이지 적용` |
| 10 | 담기 페이지에서 하지 말 것 4항목 |
| 11 | 미확정 3항목 |

**v2에서 내린 원칙 충돌 판단** (근거는 각 장 본문)

| 충돌 | 결정 |
|---|---|
| 「그림자는 결과 카드에만」 vs 검색 결과 카드 | 검색 결과 카드에 **그림자를 쓰지 않는다.** 1px 테두리 (5.2) |
| 「빨강은 아껴 쓴다」 vs `.chip.is-active` | **선택 상태로 인정한다.** tint 배경 + 빨강 테두리 (5.2) |
| 「빨강은 아껴 쓴다」 vs `.place-card__save.is-saved` | **선택 상태가 아니다.** 담기 전에만 빨강을 쓰고 담은 뒤에는 거둔다 (5.2) |
| 「한 화면에 하나씩」 vs `.results` PC 3열 | 원칙은 **결정층에만** 적용된다. 담기 페이지는 탐색층이므로 3열 (1.5) |
| 담아둔 개수를 빨강으로 쓸 것인가 | **쓰지 않는다.** 재방문율과 달리 판단 재료가 아니다 (5.2) |

---

## 0. 결정 요약

| 항목 | 결정 |
|---|---|
| 무드 | 따뜻하고 친근 — 음식답게 생기 있게 |
| 배경 | 흰색 (`#FFFFFF`) |
| 메인 컬러 | 토마토 레드 |
| 폰트 | Inter + Pretendard 단일 스택, 위계는 굵기로만 |
| 레퍼런스 | 토스 — 큰 타이포, 명확한 위계, 한 화면에 하나씩 |
| 담기 페이지 성격 (v2) | 탐색층 — 후보를 늘어놓고 비교하는 화면. 결정층인 체험 데모와 반대 |
| 담기 페이지 폭 (v2) | `.container--wide` 480 / 768 / 1080. 결과는 1열 → 2열 → 3열 |

---

## 1. 디자인 원칙

**① 한 화면에 하나씩**
질문도, 메시지도, 결정도 한 번에 하나만 보여준다. 체험 데모의 질문 3개는 절대 한 화면에 함께 두지 않는다.

**② 위계는 굵기와 크기로만**
색을 여러 개 써서 위계를 만들지 않는다. 컬러는 위계 수단이 아니라 강조 수단이다.

**③ 온기는 색이 아니라 여백·라운드·말투에서**
배경이 흰색이므로 색으로 따뜻함을 낼 수 없다. 넉넉한 여백, 부드러운 라운드, 사람이 말하듯 쓴 문장으로 온기를 만든다.

**④ 빨강은 아껴 쓴다**
토마토 레드는 넓은 면적에 깔면 공격적으로 읽힌다. CTA 버튼, 선택된 상태, 결과 카드의 핵심 수치에만 쓴다.

### 1.5 원칙의 적용 범위 — 결정층과 탐색층 (v2)

원칙 ①「한 화면에 하나씩」은 **모든 화면에 적용되는 규칙이 아니다.** 어느 층의 화면인지에 따라 갈린다.
PRD 1장은 서비스를 두 층으로 나눈다.

| 층 | 하는 일 | 해당 화면 | 좋은 화면이란 |
|---|---|---|---|
| **결정층** | 오늘 갈 한 곳을 고름 | 체험 데모 | 선택지를 **줄여준다** |
| **탐색층** | 갈 만한 곳을 찾아 담을지 판단 | 맛집 담기 페이지 | 후보를 **비교하게 해준다** |

체험 데모가 520px 단일 컬럼에 갇혀 있는 이유는 폭이 좁아야 예뻐서가 아니라,
**한 번에 하나만 보여주는 것이 결정층의 기능 자체**이기 때문이다.

담기 페이지는 반대다. 검색 결과 10건을 한 번에 하나씩 넘겨 보게 하면
"어느 게 나은가"를 판단할 수 없어 화면이 제 일을 못 한다.
그래서 담기 페이지는 `.container--wide`를 쓰고 결과를 PC에서 3열로 편다.

> **원칙 ①은 폐기되지 않는다. 적용 범위가 명시됐을 뿐이다.**
> 담기 페이지 안에서도 결정의 순간 — 담기 버튼을 누르는 지점 — 은 카드 하나당 하나로 유지한다.
> 카드 안에 담기·해제·태그·메모를 한꺼번에 늘어놓지 않는다.

원칙 ②(위계는 굵기와 크기로만)와 ④(빨강은 아껴 쓴다)는 **층과 무관하게 그대로 적용된다.**
탐색층이라고 색으로 위계를 만들거나 빨강을 넓게 쓰지 않는다.

---

## 2. 컬러

### 토큰

```css
:root {
  /* Primary — 토마토 레드 (CTA·강조 전용) */
  --color-primary:        #D93A2B;
  --color-primary-hover:  #BE2E20;
  --color-primary-press:  #A32619;
  --color-primary-tint:   #FDF0EE;  /* 선택된 상태의 배경 */

  /* Surface */
  --color-bg:             #FFFFFF;
  --color-surface-alt:    #FAF8F7;  /* 섹션 교차 배경 (아주 옅은 웜톤) */
  --color-line:           #EDE9E7;

  /* Text — 순검정 대신 따뜻한 다크그레이 */
  --color-ink-900:        #1A1614;  /* 헤드라인 */
  --color-ink-700:        #4A423E;  /* 본문 */
  --color-ink-500:        #7C726C;  /* 보조 설명 */
  --color-ink-300:        #C9C2BD;  /* 비활성 */

  /* Semantic — 브랜드 레드와 반드시 구분 */
  --color-error:          #B3261E;  /* 더 어둡고 채도 낮음 */
  --color-warning:        #B45309;  /* 앰버 계열 */
  --color-success:        #2E7D5B;
}
```

### 사용 규칙

| 컬러 | 쓰는 곳 | 쓰지 않는 곳 |
|---|---|---|
| `primary` | CTA 버튼 배경, 선택된 선택지 테두리, 재방문율 수치 | 큰 배경 면적, 본문 텍스트, 아이콘 전반 |
| `primary-tint` | 선택된 선택지의 배경 | 섹션 배경 |
| `surface-alt` | 섹션이 연달아 붙을 때 구분용 | 카드 내부 |
| `ink-900` | 헤드라인, 맛집 이름 | 긴 본문 (무겁게 읽힌다) |

### 주의

- **본문에 `#000000`을 쓰지 않는다.** 흰 배경과의 대비가 지나쳐 차갑게 읽힌다.
- **에러는 색만으로 구분하지 않는다.** 브랜드 레드와 혼동되므로 아이콘 + 문구를 반드시 함께 쓴다.
- 위 값들은 흰 배경 기준 WCAG AA(4.5:1)를 목표로 잡았다. 실제 구현 시 대비를 한 번 확인할 것.

### 2.4 실제 배경 기준 대비표 (v2)

**토큰 표의 값을 그대로 믿으면 안 된다.** 위 목표치는 흰 배경 기준이고,
`surface-alt`(#FAF8F7)나 `primary-tint`(#FDF0EE) 위에 얹으면 떨어진다.
아래는 실측값이다. **굵은 칸은 AA(4.5:1) 미달이므로 쓰지 않는다.**

| 글자 ↓ · 배경 → | `bg` #FFFFFF | `surface-alt` #FAF8F7 | `primary-tint` #FDF0EE |
|---|---|---|---|
| `ink-900` | 17.97 | 16.97 | 16.15 |
| `ink-700` | 9.81 | 9.27 | 8.82 |
| `ink-500` | 4.69 | **4.43** | **4.21** |
| `primary` | 4.57 | **4.32** | **4.11** |
| `primary-hover` | 5.83 | 5.51 | 5.24 |
| `primary-press` | 7.37 | 7.05 | 6.63 |
| `error` | 6.54 | 6.17 | 5.88 |
| `#FFFFFF` | — | — | — |

`#FFFFFF` 글자는 `primary` 4.57 · `primary-hover` 5.83 · `primary-press` 7.37 · `ink-900` 17.97이다.

**여기서 따라 나오는 규칙**

- **`surface-alt`·`primary-tint` 위에는 `ink-500`을 쓰지 않는다.** `ink-700`으로 올린다. 위계는 굵기와 크기로 유지한다 (원칙 ②).
- **`primary-tint` 위에는 `primary`를 쓰지 않는다.** `primary-hover`로 한 단계 내린다.
- 흰 배경 위 `primary`는 4.57로 통과하지만 여유가 0.07뿐이다. 작은 글씨(15px 이하)에는 `primary-hover`를 쓴다. 테두리·아이콘 등 비텍스트에는 `primary` 그대로 쓴다 (비텍스트 기준은 3:1).
- 이 규칙 때문에 토큰에서 벗어난 곳은 **CSS에 사유를 주석으로 남긴다.** 현재 다섯 곳이다 — `.badge--wish` · `.badge--live` · `.footer__meta` · `.chip.is-active` · `.place-card__save`.

---

## 3. 타이포그래피

### 폰트 스택

```css
font-family: 'Inter', 'Pretendard', -apple-system, BlinkMacSystemFont, sans-serif;
```

- 라틴·숫자 → **Inter**
- 한글 → **Pretendard** (Inter 구조 기반이라 나란히 놓아도 이질감 없음)
- 굵기는 **400 / 600 / 700 세 가지만** 사용한다.

### 스케일

| 이름 | 모바일 | 데스크톱 | 굵기 | 행간 | 자간 | 용도 |
|---|---|---|---|---|---|---|
| Display | 32px | 44px | 700 | 1.30 | -0.03em | Hero 헤드라인 |
| H1 | 24px | 28px | 700 | 1.35 | -0.02em | 섹션 제목, 질문 |
| H2 | 20px | 22px | 600 | 1.40 | -0.02em | 카드 제목, 맛집 이름 |
| Body-L | 17px | 18px | 400 | 1.60 | -0.01em | 서브카피, 버튼 라벨 |
| Body | 15px | 16px | 400 | 1.60 | -0.01em | 본문 |
| Caption | 13px | 13px | 600 | 1.50 | 0 | 배지, 진행 표시 |

### 규칙

- **PC(1024px~)에서는 Display 52px, H1 32px로 한 단계 키운다.** 1080px 컨테이너에서 44px Display는 작게 읽힌다.
- **한글에는 음수 자간이 필수다.** 기본 자간은 한글에서 헐거워 보인다.
- 한 화면에 Display는 하나만 쓴다.
- 긴 문장을 굵게 쓰지 않는다. 강조는 한 줄 이내에서만.
- 줄바꿈은 의미 단위로 끊는다. (`저장은 했는데,` / `오늘 어디 가지?`)

---

## 4. 레이아웃 · 스페이싱

### 브레이크포인트

| 이름 | 범위 |
|---|---|
| 모바일 | ~767px |
| 태블릿 | 768~1023px |
| PC | 1024px~ |

### 컨테이너 — 두 종류

용도에 따라 나눠 쓴다. **집중이 필요한 곳은 좁게, 카드를 늘어놓는 곳은 넓게.**

| | 모바일 | 태블릿 | PC | 쓰는 곳 |
|---|---|---|---|---|
| `.container` (읽는 컬럼) | 480px | 600px | 640px | 체험 데모, 마무리 CTA |
| `.container--wide` (배치 컬럼) | 480px | 768px | 1080px | Hero, 공감, 해결, 기능, 푸터 |

좌우 패딩은 20px → 32px(태블릿) → 40px(PC).

> **개정 이력 (2026-08-14)** — 원안은 "데스크톱에서도 480px 단일 컬럼 유지"였다. 실제 구현 후 넓은 화면에서 좌우 여백이 과도해 반응형으로 개정했다.
> 다만 **체험 데모는 화면 폭과 무관하게 520px 단일 컬럼을 유지한다.** 「한 화면에 하나씩」이 이 섹션의 존재 이유이므로 여기서는 넓히지 않는다.

#### 4.5 담기 페이지 컨테이너 (v2)

담기 페이지는 별도 래퍼 없이 `<body>`가 컨테이너 역할을 한다.
`UI-CONTRACT.md` 「반응형」이 정한 대로 **세 클래스를 함께** 건다.

```html
<body class="save-page container container--wide">
```

**`.container--wide`는 단독 클래스가 아니라 modifier다.** `max-width`(480 / 768 / 1080)만 갖고 있고
`width: 100%` · `margin: 0 auto` · 좌우 패딩(20 / 32 / 40)은 `.container` 쪽에 있다.
`--wide`만 붙이면 정렬과 여백이 통째로 빠진다.

→ **`.save-page`는 `max-width`·좌우 패딩·가로 마진·`width`를 다시 선언하지 않는다.**
브레이크포인트 폭의 진실의 원천은 `style.css`의 `.container` / `.container--wide` 하나뿐이다.
같은 숫자를 두 곳에 적으면 나중에 한쪽만 고쳐지고 어긋난다.

`.save-page`가 더하는 것은 **세로 여백뿐이다.** 하단 96px(PC 120px) —
화면 하단에 고정된 토스트가 마지막 항목을 가리기 때문이다.
`.container`의 `padding: 0 20px` 단축 선언이 `padding-bottom`을 0으로 만들므로
`save.css`에서 `padding-bottom`만 따로 덮어쓴다. `save.css`가 뒤에 오므로 이 값이 이긴다.

### 스페이싱 스케일 (4px 기반)

`4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96`

| 위치 | 값 |
|---|---|
| 섹션 상하 여백 | 64px (모바일) / 96px (태블릿) / 120px (PC) |
| 요소 간 기본 간격 | 16px |
| 카드 내부 패딩 | 24px |
| 제목과 본문 사이 | 12px |

### 라운드 · 그림자

```css
--radius-sm:   8px;   /* 배지 */
--radius-md:  14px;   /* 버튼, 선택지 */
--radius-lg:  20px;   /* 카드 */
--radius-full: 999px;

--shadow-card:   0 1px 2px rgba(26,22,20,0.04), 0 8px 24px rgba(26,22,20,0.06);
--shadow-result: 0 2px 4px rgba(26,22,20,0.05), 0 16px 40px rgba(26,22,20,0.10);
```

그림자는 결과 카드에만 강하게 쓴다. 나머지는 `--color-line` 1px 테두리로 구분한다.

---

## 5. 컴포넌트 스펙

### Primary 버튼 (CTA)

| 속성 | 값 |
|---|---|
| 높이 | 56px |
| 너비 | 컨테이너 100% (태블릿부터 최대 320px) |
| 배경 | `--color-primary` |
| 텍스트 | `#FFFFFF`, Body-L, 700 |
| 라운드 | `--radius-md` |
| hover | `--color-primary-hover` |
| active | `--color-primary-press` + `scale(0.98)` |

### 선택지 버튼 (체험 데모 Q1~Q3)

| 상태 | 스타일 |
|---|---|
| 기본 | 흰 배경, `--color-line` 1px 테두리, `ink-900` 텍스트, 높이 60px |
| hover | 테두리 `ink-300` |
| 선택됨 | 배경 `--color-primary-tint`, 테두리 `--color-primary` 1.5px |

- 세로로 쌓고 간격 12px.
- 선택 즉시 다음 질문으로 넘어가므로 **"선택됨" 상태는 250ms만 보인다.** 사용자가 자기 선택을 인지할 최소 시간이다.

### 진행 표시

- 질문 영역 상단에 `1 / 3` (Caption, `ink-500`)
- 그 아래 높이 3px 프로그레스 바, 채워진 부분 `--color-primary`, 라운드 `full`

### 결과 카드

```
┌─────────────────────────────┐
│ [카테고리]                   │  Caption, ink-500
│ 담백한식탁                    │  H2, ink-900
│ #혼밥 #가까움 #가성비          │  Caption, ink-500
│                             │
│ "혼자 가서 20분 만에          │  Body, ink-700
│  먹고 나오기 좋아요"           │
│                             │
│ ─────────────────────────── │  line
│ 또 갈 것 같아요  88%          │  숫자만 primary + 700
└─────────────────────────────┘
   혼자 가기 좋고, 가까워서 골랐어요   ← Caption, ink-500, 카드 밖
```

- `--radius-lg`, `--shadow-result`, 패딩 24px
- `status: wish`인 경우 하단 재방문율 대신 **`아직 안 가본 곳` 배지**를 표시한다.

### 배지

| 종류 | 스타일 |
|---|---|
| `준비 중` | 배경 `surface-alt`, 텍스트 `ink-500`, Caption, `--radius-full`, 패딩 4/10px |
| `아직 안 가본 곳` | 배경 `primary-tint`, 텍스트 `primary`, 동일 형태 |
| `써볼 수 있어요` (v2) | 배경 `primary-tint`, 텍스트 `primary-hover`, 동일 형태 |

> `아직 안 가본 곳`의 텍스트는 명세상 `primary`지만 tint 위에서 4.11로 AA에 못 미친다.
> 구현은 `primary-hover`(5.24)를 쓴다 (2.4장). 명세를 고치지 않고 예외로 두는 이유는
> 다른 배경에 얹힐 가능성을 남겨두기 위해서다.

---

## 5.2 담기 페이지 컴포넌트 (v2)

클래스 이름은 `UI-CONTRACT.md`가 정본이다. 여기서는 **수치와 판단 근거**만 적는다.

### 검색 폼 (`.search__form`)

| 속성 | 값 |
|---|---|
| 배치 | 입력과 버튼을 가로로, 간격 8px |
| 입력 높이 | 52px (태블릿부터 56px) |
| 입력 글자 | **16px 400** — 15px로 내리면 iOS Safari가 포커스 시 화면을 확대한다 |
| 입력 기본 | 흰 배경, `--color-line` 1px 테두리, `--radius-md` |
| 입력 hover / focus | 테두리 `ink-300` / `primary` |
| placeholder | `ink-500` (흰 배경 4.69) |
| 제출 버튼 | 배경 `primary`, 흰 글자 16px 700, `--radius-md`, 좌우 패딩 20px |
| 폼 최대 폭 (PC) | **640px** — 결과는 1080px로 펴되 입력 줄은 여기서 끊는다 |

- PC 폼 최대 폭 640px는 `.container`의 PC 폭과 값이 같지만 **거기서 파생된 것이 아니다.** 입력 폼의 독립적인 컴포넌트 제약이며, `.container`가 바뀌어도 자동으로 따라가지 않는다. 담기 페이지에서 `style.css`의 값을 따라가는 것은 `<body>`의 컨테이너 폭 하나뿐이다.
- 제출 버튼은 **이 페이지의 유일한 채워진 빨강 버튼**이다. `.btn-primary`(56px 고정폭 320px)와 달리 폼 안 인라인이므로 별도 수치를 쓴다.
- CTA 최대폭 320px 규칙은 `.action`에 담긴 독립 CTA에만 적용된다. 폼 인라인 버튼은 해당하지 않는다.

### 카테고리 칩 (`.chip`)

| 상태 | 스타일 |
|---|---|
| 기본 | 흰 배경, `--color-line` 1px, `ink-700` 15px 600, `--radius-full`, 높이 44px, 좌우 패딩 16px |
| hover | 테두리 `ink-300` |
| `.is-active` | 배경 `primary-tint`, 테두리 **2px** `primary`, `primary-hover` 15px **700**, 좌우 패딩 15px |

**판단 ① — 활성 칩은 「선택 상태」인가 → 그렇다.**
원칙 ④가 허용하는 "선택된 상태"에 정확히 해당한다. 데모의 선택지 버튼(`.choice.is-selected`)과 같은
시각 언어(tint 배경 + 빨강 테두리)를 쓴다. **같은 의미에는 같은 형태를 준다.**

**판단 ② — 색만으로 구분하지 않는다.**
배경(흰 → tint) · 테두리 굵기(1 → 2px) · 굵기(600 → 700) 세 가지가 함께 바뀐다.
흑백으로 출력해도 어느 칩이 켜졌는지 읽힌다.

**판단 ③ — 활성 시 패딩을 1px 줄인다.**
테두리가 1px 두꺼워지면 칩 폭이 2px 늘어 옆 칩들이 밀린다.
좌우 패딩을 16 → 15px로 줄여 총 폭을 고정한다. 선택이 레이아웃을 흔들면 안 된다.

### 상태 표시 (`.search__status`)

**평상시에는 자리를 차지하지 않는다.** 모디파이어가 없으면 `display: none`이다.
빈 텍스트 여부가 아니라 **클래스로 판정**한다 — 공백 문자가 남아도 빈 박스가 새지 않는다.

공통: 위 여백 24px, 패딩 12/16px, 15px 400, `--radius-md`. 한 줄 높이이므로 나타나도 아래가 크게 밀리지 않는다.

| 상태 | 배경 | 글자 | 형태로 구분되는 지점 |
|---|---|---|---|
| `--loading` | `surface-alt` | `ink-700` (9.27) | 문구 뒤 4px 점이 1.2초 주기로 깜박인다 |
| `--empty` | `surface-alt` | `ink-700` (9.27) | 테두리 없는 담담한 면 |
| `--error` | `bg` (흰색) | `error` 600 (6.54) | 1px 테두리 + **왼쪽 4px 굵은 띠** |

**판단 ④ — 에러를 색만으로 구분하지 않는다.**
`error`(#B3261E)는 브랜드 레드(#D93A2B)와 붉은 계열이 겹친다. 색만 믿으면 브랜드 강조와 헷갈린다.
그래서 에러에만 **테두리를 두르고 왼쪽 띠를 굵게** 준다. 다른 두 상태는 테두리가 없으므로 형태만으로 갈린다.
문구도 서버 메시지를 그대로 노출하므로 내용으로도 구별된다.

- 로딩의 점은 `::before/::after`로 넣는다. 낭독기에는 읽히지 않으므로 정보를 점에만 싣지 않는다 — 문구가 정보를 진다.
- `prefers-reduced-motion`에서 점의 깜박임을 멈추고 불투명하게 고정한다.

### 검색 결과 카드 (`.place-card`)

4단 계층. 위에서 아래로 굵기와 크기가 오르내리며 눈이 이름에 먼저 닿게 한다.

```
┌─────────────────────────────┐
│ 한식                          │  Caption 13/600  ink-500   ← 분류
│ 담백한식탁                     │  H2      20/600  ink-900   ← 이름 (시선의 목적지)
│ 서울 중구 을지로 12            │  Body    15/400  ink-700   ← 주소
│ 320m                         │  Caption 13/600  ink-500   ← 거리
│                             │
│ 상세 보기            [ 담기 ] │  ← 아래에 붙여 정렬
└─────────────────────────────┘
```

| 속성 | 값 |
|---|---|
| 배경 | `bg` |
| 테두리 | `--color-line` 1px |
| 그림자 | **없음** |
| 라운드 | `--radius-lg` |
| 패딩 | 24px |
| hover | 테두리 `ink-300` |
| 요소 간격 | 분류→이름 4px · 이름→주소 8px · 주소→거리 4px · 거리→버튼줄 16px |
| 버튼줄 | 카드 아래에 붙인다. 카드 높이가 달라도 버튼 줄은 같은 선에 정렬된다 |

**판단 ⑤ — 검색 결과 카드에 그림자를 쓰지 않는다.**

4장은 "그림자는 결과 카드에만 강하게 쓴다"고 못 박았다. 그 `결과 카드`는 데모의 결과 카드 —
**화면에 단 하나 존재하는, 오늘의 답** — 을 가리킨다. 그림자가 그 하나를 화면에서 띄우는 역할을 한다.

검색 결과 카드는 성격이 정반대다. 10건이 3열로 깔리는 **후보 목록**이다.
여기에 `--shadow-card`를 얹으면 두 가지가 동시에 무너진다.

1. 화면에 그림자 진 카드가 10장 생기면 데모 결과 카드의 "여기가 답이다"라는 신호가 값을 잃는다.
   그림자는 흔해지는 순간 위계 수단이기를 그만둔다.
2. 10장의 그림자가 겹쳐 화면 전체가 지저분해진다. 10장에서 "하지 말 것"으로 이미 금지한 상태다.

→ **`--shadow-card` 토큰은 담기 페이지에서 쓰지 않는다.** 1px 테두리로만 구분하고,
hover에서 테두리 색만 `ink-300`으로 바꾼다. 그림자 위계는 이렇게 유지된다.

| 무게 | 무엇 | 구분 수단 |
|---|---|---|
| 1위 | 데모 결과 카드 (오늘의 한 곳) | `--shadow-result` |
| 2위 | 검색 결과 카드 (후보) | 1px 테두리 + 라운드 20px + 패딩 24px |
| 3위 | 담아둔 목록 항목 (보관함) | 아래쪽 1px 구분선만 |

### 담기 버튼 상태 전환 (`.place-card__save`)

| 상태 | 라벨 | 배경 | 테두리 | 글자 |
|---|---|---|---|---|
| 기본 | `담기` | `bg` (흰색) | `primary` 1px | `primary-hover` 15px **700** |
| hover | | `primary-tint` | `primary` 1px | 동일 |
| `.is-saved` | `담았어요` | `surface-alt` | `--color-line` 1px | `ink-700` 15px **400** |
| `.is-saved` hover | | 동일 | `ink-300` | `ink-900` |

최소 44×44px. `.is-saved`는 비활성이 아니라 **담기 해제로 동작한다** — 상태는 `aria-pressed`가 진다.

**판단 ⑥ — 담기 전 버튼을 채운 빨강으로 하지 않는다.**
`.btn-primary`처럼 면을 빨강으로 채우면, PC 3열 화면에서 **빨강 덩어리가 9~10개 반복된다.**
면적 총합으로 보면 원칙 ④가 금지한 "넓은 면적"과 다를 바 없다.
→ 빨강을 **선(테두리)으로만** 쓴다. 페이지에서 채워진 빨강은 검색 제출 버튼 하나뿐이다.

**판단 ⑦ — `.is-saved`는 「선택 상태」가 아니다. 빨강을 거둔다.**
칩의 `.is-active`는 "지금 이 조건으로 보고 있다"는 **현재 진행 중인 선택**이라 강조가 필요하다.
반면 `담았어요`는 **이미 끝난 일**이다. 끝난 일에 강조를 남겨두면
화면에서 눈길을 끄는 것이 "앞으로 할 수 있는 일"이 아니라 "이미 한 일"이 된다.

→ 담기 전에 빨강을 쓰고, 담은 뒤에는 중립 면으로 눕힌다. 시선은 아직 안 담은 카드로 흐른다.

**판단 ⑧ — 색 말고도 세 가지가 함께 바뀐다.**
배경(흰 → 회색) · 테두리 색(빨강 → 라인) · 굵기(700 → 400).
색을 다 지우고 흑백으로 봐도 두 상태는 명확히 다르다.
체크 표시(`✓`)는 넣지 않는다 — `aria-pressed`가 이미 상태를 알리므로 낭독이 겹치고,
위 세 변화만으로 시각 구분이 충분하다.

### 담아둔 목록 (`.saved-item`)

**검색 결과 카드와 같은 무게로 보이면 안 된다.** 카드는 "판단하는 자리",
담아둔 목록은 "이미 끝난 것의 보관함"이다.

| | 검색 결과 카드 | 담아둔 목록 항목 |
|---|---|---|
| 형태 | 테두리 있는 박스 | 박스 없음. 아래쪽 1px 구분선만 |
| 패딩 | 24px | 위아래 12px, 좌우 0 |
| 이름 | 20/22px 600 | **16/17px 600** |
| 배치 | 세로 4단 | 가로 1줄 (이름·분류 / 해제 버튼) |
| 라운드 | 20px | 없음 |

- 섹션 자체는 위 여백 48px(태블릿 64px) + 위쪽 1px 구분선으로 검색 결과와 갈라놓는다.
- 배경은 흰색 그대로 둔다. `surface-alt` 패널로 만들면 `ink-500`이 4.43으로 AA에 걸려(2.4장) 보조 텍스트를 전부 `ink-700`으로 올려야 하고, 그러면 위계가 도리어 무거워진다.
- 개수(`.saved__count`)는 `ink-500`. 굵기 600으로 제목과 구분한다.
- 해제 버튼은 44×44px 이상, `--color-line` 1px 테두리 + `ink-700` 13/600. hover에서 테두리 `ink-300`, 글자 `ink-900`.
- 목록이 비면 제목 아래에 `아직 담아둔 곳이 없어요` 한 줄을 둔다. 제목만 남고 아래가 텅 비지 않게 한다.

**판단 ⑨ — 이름에 `.h2` 유틸이 붙지만 크기를 낮춘다.**
계약서상 마크업은 `class="h2 saved-item__name"`이다. 그대로 두면 20/22px이 되어
검색 결과 카드 제목과 **같은 크기**가 된다. 그러면 위 표의 위계가 성립하지 않는다.
→ `.h2` 유틸 자체는 건드리지 않고 `.saved-item__name`에서만 16/17px로 내린다.
CSS에 사유를 주석으로 남긴다.

**판단 ⑩ — 개수를 빨강으로 쓰지 않는다.**
원칙 ④가 허용하는 수치는 "결과 카드의 핵심 수치" — 재방문율처럼 **판단의 재료가 되는 숫자**다.
담아둔 개수는 판단 재료가 아니라 단순 집계다. 빨강을 붙일 근거가 없다. `ink-500`으로 둔다.

### 토스트 (`.toast`)

| 속성 | 값 |
|---|---|
| 위치 | 화면 하단 고정, 가로 중앙. 아래 24px + `env(safe-area-inset-bottom)` |
| 최대 폭 | `calc(100% - 40px)` |
| 배경 · 글자 | `ink-900` / `#FFFFFF` (17.97) |
| 패딩 · 라운드 | 12/20px · `--radius-full` |
| 글자 | 15px 600 |
| 등장 | fade + `translateY(8px → 0)` 250ms `--ease` |
| 퇴장 | **없다.** 즉시 사라진다 |
| 권장 지속 | 2.4초 |

- 표시 여부는 JS가 `hidden` 속성으로 제어한다. 따라서 CSS는 `.toast[hidden] { display: none; }`을 **반드시 명시한다.** `.toast { display: flex }`가 UA 스타일시트의 `[hidden]`을 이기므로, 이 한 줄이 없으면 토스트가 영영 사라지지 않는다.
- 퇴장 애니메이션이 없는 것은 `hidden`이 붙는 순간 요소가 렌더 트리에서 빠지기 때문이다. 퇴장 모션이 필요해지면 JS가 클래스로 제어해야 하므로 계약 변경이 선행되어야 한다.
- 토스트 배경은 페이지에서 유일한 어두운 면이다. **빨강으로 하지 않는다** — 담김은 성공이지 경고가 아니고, 원칙 ④의 허용 목록에도 없다.
- `prefers-reduced-motion`에서 `translateY`를 빼고 fade만 남긴다.

---

## 6. 모션

```css
--ease:     cubic-bezier(0.2, 0, 0, 1);
--dur-fast: 150ms;
--dur-base: 250ms;
```

| 대상 | 동작 |
|---|---|
| 질문 전환 | 이전 질문 fade-out 150ms → 다음 질문 fade-in + `translateY(8px → 0)` 250ms |
| 결과 카드 등장 | fade-in + `translateY(16px → 0)` 300ms |
| 버튼 press | `scale(0.98)`, 100ms |

- 화면 전체가 움직이는 연출은 쓰지 않는다.
- `prefers-reduced-motion: reduce`에서는 모든 transform을 제거하고 fade만 남긴다.

---

## 7. 카피 톤앤매너

**원칙: 광고 문구가 아니라 옆자리 동료가 하는 말처럼.**

| 항목 | 규칙 |
|---|---|
| 어체 | 존댓말 (`~해요` 중심, `~합니다`는 최소화) |
| 길이 | 한 문장 한 메시지. 두 줄을 넘기지 않는다 |
| 느낌표 | 쓰지 않는다. 친근함은 어휘로 만든다 |
| 과장 | `최고의`, `완벽한`, `혁신적인` 금지 |
| 금지어 | **`AI 추천`** — Phase 0의 추천은 규칙 기반 필터링이다 (PRD 4장) |

**예시**

| ❌ | ✅ |
|---|---|
| 최적의 맛집을 AI가 추천해드립니다! | 담아둔 곳 중에서 오늘 갈 한 곳을 골라드려요 |
| 조건에 맞는 결과가 없습니다 | 조건을 조금 넓혀서 골랐어요 |
| 지금 바로 시작하세요! | 오늘 갈 곳 골라보기 |

### 7.2 담기 페이지 문구 (v2)

| 자리 | 문구 |
|---|---|
| 헤더 제목 | `맛집 담기` |
| 헤더 설명 | `가보고 싶은 곳을 검색해서 목록에 담아두세요` |
| 입력 placeholder | `가게 이름이나 지역을 입력하세요` |
| 로딩 | `찾아보는 중이에요` |
| 0건 | `검색 결과가 없어요. 다른 이름으로 찾아보세요` |
| 에러 | 서버가 준 메시지를 그대로 노출 |
| 담기 버튼 | `담기` → `담았어요` |
| 목록이 빔 | `아직 담아둔 곳이 없어요` |
| 토스트 | `담았어요` / `담기를 해제했어요` |

- 검색은 **탐색층**의 기능이다. 카피에서 `골라드려요`류를 쓰지 않는다 — 여기서 고르는 주체는 사람이다.
- `검색 결과가 없습니다`가 아니라 `없어요`. 그리고 **다음에 할 일을 붙인다.** 막다른 길로 끝내지 않는다.
- 검색은 규칙 기반 키워드 조회다. `똑똑한`·`추천`·`AI` 같은 말을 붙이지 않는다.

### 7.3 랜딩 → 담기 페이지 진입 문구 (v2)

| 자리 | 문구 |
|---|---|
| ⑤ 기능 미리보기 · 맛집 담기 배지 | `써볼 수 있어요` |
| ⑤ 기능 미리보기 · 맛집 담기 링크 | `담으러 가기` |
| ① Hero CTA 아래 보조 링크 | `먼저 맛집부터 담을래요` |
| ⑥ 마무리 CTA 아래 보조 링크 | `가보고 싶은 곳 담아두기` |

- ⑤ 섹션 제목은 `앞으로 이런 것들을 준비하고 있어요` → `이런 것들을 하나씩 열고 있어요`로 고쳤다. 카드 하나가 실제로 열렸는데 제목이 전부 준비 중이라고 말하면 문구가 거짓이 된다.
- ⑤ 맛집 담기 설명도 `링크 하나로 목록에 추가해요` → `검색해서 가보고 싶은 곳을 담아둬요`로 고쳤다. 실제 구현이 검색 기반이다. **지금 못 지키는 약속을 카피에 넣지 않는다**(7장 금지어 원칙)는 규칙은 기능 설명에도 똑같이 적용된다.

---

## 8. 화면별 적용 (PRD 5장 대응)

| 섹션 | 배경 | 핵심 타이포 | 비고 |
|---|---|---|---|
| ① Hero | `bg` | Display | CTA 버튼 하나 + 보조 텍스트 링크. 화면 하단에 스크롤 힌트. **PC에서 2단** (카피 \| 결과 카드 미리보기) |
| ② 문제 공감 | `surface-alt` | H1 + Body | 아이콘 없이 텍스트만. **태블릿부터 3컬럼** |
| ③ 해결 방식 | `bg` | H2 ×3 | 모바일은 세로 스택, **태블릿부터 3컬럼** |
| ④ 체험 데모 | `bg` | H1 (질문) | **이 섹션만 화면 높이를 꽉 채운다.** 폭은 항상 520px 단일 컬럼 |
| ⑤ 기능 미리보기 | `surface-alt` | H2 + Caption | 각 항목에 `준비 중` 배지. **태블릿부터 2×2** |
| ⑥ 마무리 CTA | `bg` | H1 | Hero와 같은 버튼 스타일. 태블릿부터 중앙 정렬 |
| ⑦ 푸터 | `surface-alt` | Caption | **PC에서 좌우 배치** |

**공통 규칙**
- CTA 버튼은 태블릿부터 최대 320px. 넓은 화면에서 컨테이너 전체 폭으로 늘리지 않는다.
- Hero의 결과 카드 미리보기는 PC에서만 노출한다. 좁은 화면에서는 바로 아래 데모와 중복된다.

### 8-B. 담기 페이지 적용 (v2)

| 영역 | 배경 | 핵심 타이포 | 비고 |
|---|---|---|---|
| 헤더 | `bg` | H1 | 위에 `← 오늘은 여기` 되돌아가기 링크 |
| 검색 폼 | `bg` | — | 입력 + 제출 버튼 가로 배치. 화면 폭과 무관하게 한 줄 |
| 카테고리 칩 | `bg` | — | 가로 배치, 넘치면 줄바꿈 |
| 상태 표시 | `surface-alt` / `bg`(에러) | Body | 모디파이어 없으면 자리를 차지하지 않음 |
| 검색 결과 | `bg` | H2 (이름) | **모바일 1열 → 태블릿 2열 → PC 3열** |
| 담아둔 곳 | `bg` | H2 (제목) | 위쪽 1px 구분선으로 검색 결과와 분리. 항상 1열 |
| 토스트 | `ink-900` | — | 화면 하단 고정 |

**섹션 배경을 교차시키지 않는다.**
랜딩은 섹션이 7개라 `surface-alt`로 경계를 만들어야 했다. 담기 페이지는 영역이 적고
`.save-page`가 최대 1080px로 잘려 있어 전면 배경이 어차피 깔리지 않는다.
경계는 여백과 1px 구분선으로 충분하다.

**랜딩에서 담기 페이지로 가는 입구는 두 곳이다.**

| 위치 | 이유 |
|---|---|
| ① Hero CTA 아래 보조 텍스트 링크 | 담아둔 곳이 아직 없는 첫 방문자의 다음 행동 |
| ⑤ 기능 미리보기 · `맛집 담기` 카드 | 그 카드가 이 기능을 설명하는 자리다. 배지가 `준비 중`으로 남아 있으면 문구가 거짓이 된다 |
| ⑥ 마무리 CTA 아래 보조 텍스트 링크 | 데모를 끝낸 사람에게 제시하는 다음 행동 |

**Hero의 입구는 텍스트 링크다. 두 번째 버튼을 세우지 않는다.**
랜딩의 주인공은 여전히 ④ 체험 데모다(PRD 5장) — 주 CTA `오늘 갈 곳 골라보기`가 그리로 데려간다.
같은 굵기의 버튼을 하나 더 세우면 랜딩의 핵심 전환이 갈라지므로, 담기는 `.link-text`로
**한 단계 낮춰** 둔다. ⑥의 처리와 같은 규칙이다.

> **개정 사유.** 초판은 Hero에 담기 입구를 아예 넣지 않았다. 담아둔 곳이 하나도 없는
> 첫 방문자에게 `골라드려요`가 **아직 지킬 수 없는 약속**이기 때문에 입구를 하나 열었다.
> 금지의 원래 목적(전환이 갈라지는 것)은 위계로 지킨다 — 버튼은 여전히 하나뿐이다.

---

## 9. 접근성 체크리스트

- [ ] 모든 텍스트가 배경 대비 4.5:1 이상 (Caption 포함)
- [ ] 선택지 버튼의 터치 영역이 최소 44×44px
- [ ] 선택 상태를 색만으로 구분하지 않는다 (테두리 굵기도 함께 변경)
- [ ] 키보드 Tab으로 데모를 끝까지 진행할 수 있다
- [ ] 포커스 링을 제거하지 않는다 (`outline: 2px solid var(--color-primary)`)
- [ ] `prefers-reduced-motion` 대응
- [ ] 375px 화면에서 가로 스크롤이 없다

---

## 10. 하지 말 것

- 토마토 레드를 섹션 배경 등 넓은 면적에 쓰기
- 폰트 굵기를 4종 이상 쓰기
- 그림자를 모든 요소에 넣기
- 이모지를 UI 요소로 쓰기 (카피 안 소량은 허용)
- **체험 데모의 폭을 화면 크기에 따라 넓히기** (다른 섹션과 달리 여기만은 고정이다)
- CTA 버튼을 넓은 화면에서 컨테이너 전체 폭으로 늘리기
- 체험 데모에서 질문을 한 화면에 여러 개 보여주기

**담기 페이지 (v2)**

- **검색 결과 카드에 그림자 넣기** — 데모 결과 카드의 위계가 무너진다 (5.2 판단 ⑤)
- **담기 버튼을 채운 빨강으로 만들기** — 3열 화면에서 빨강 덩어리가 열 개 반복된다 (5.2 판단 ⑥)
- **담아둔 목록을 검색 결과 카드와 같은 형태로 만들기** — 끝난 것과 판단 중인 것이 같은 무게가 된다
- **`surface-alt`·`primary-tint` 위에 `ink-500`이나 `primary` 얹기** — AA 미달이다 (2.4장)
- **`.toast`에 `[hidden]`을 이기는 `display` 선언 남기기** — 토스트가 영영 사라지지 않는다
- **담기 페이지를 데모처럼 좁은 단일 컬럼으로 만들기** — 탐색층은 비교가 기능이다 (1.5장)

---

## 11. 미확정 사항

- [ ] 서비스명 → 확정 시 Hero·푸터 로고 타이포 규칙 추가 필요
- [ ] 로고 · 파비콘
- [ ] 결과 카드에 이미지를 넣을지 여부 (현재는 텍스트 전용)
- [ ] 다크 모드 지원 여부 (Phase 0에서는 미지원)

**담기 페이지 (v2)**

- [ ] 담아둔 곳이 수십 개로 늘어났을 때의 처리 — 접기 / 더 보기 / 검색. 현재는 전부 펼친다
- [ ] 담아둔 목록에 상태(`가볼 곳` / `가본 곳`) 배지를 붙일지 — F2(방문 기록)가 들어오는 시점에 결정
- [ ] `아직 담아둔 곳이 없어요` 빈 상태 문구를 마크업으로 옮길지 — 현재는 CSS가 들고 있다. 옮기려면 계약서에 클래스가 필요하다
